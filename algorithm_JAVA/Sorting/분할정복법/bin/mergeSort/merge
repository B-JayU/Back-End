분할 : 해결하고자 하는 문제를 작은 크기의 동일한 문제들로 분할
정복 : 각각의 작은 문제를 순환적으로 해결
합병 : 작은 문제의 해를 합하여(merge) 원래 문제에 대한 해를 구함.


MergeSort의 시간복잡도 계산하기

T(n) = 0 (if n = 1)
T(n) = T(n/2) + T(n/2) + n (otherwise)
                분할           분할     합병